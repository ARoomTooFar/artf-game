using UnityEngine;
using System.Collections;

public class RotatingFlare : MonoBehaviour {
	public float smooth = 2f;
	public float tiltAngle = 30f;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		//Quaterion target = Quaterion.Euler(tiltAroundX, 0, tiltAroundZ);
	}
}
